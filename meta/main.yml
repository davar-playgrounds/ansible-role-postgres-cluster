galaxy_info:
  author: Artem Goncharov
  description: Install PostgreSQL cluster (patroni)
  company: OpenTelekomCloud
  role_name: postgres_cluster
  license: Apache-2.0

  min_ansible_version: 2.8

  platforms:
   - name: Fedora
     versions:
      - all

  galaxy_tags: [postgres, patroni, postgresql, opentelekomcloud]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
  
